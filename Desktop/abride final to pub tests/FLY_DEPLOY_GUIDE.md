# 🚀 دليل النشر السريع على Fly.io

## ⚡ الطريقة الأسرع

بدلاً من رفع كل الملفات (بطيء جداً)، استخدم Git:

### الخطوة 1: Push للـ Git
```bash
git pull --rebase
git push
```

### الخطوة 2: Deploy من Git
```bash
flyctl deploy --app abride-app --remote-only --now
```

---

## 🎯 البديل: رابط التطبيق الحالي

إذا كان التطبيق يعمل بالفعل على Fly.io:

```
https://abride-app.fly.dev
```

افتح هذا الرابط على هاتفك للاختبار!

---

## 📱 اختبار GPS على الهاتف

1. افتح: `https://abride-app.fly.dev`
2. سجّل دخول كسائق
3. اذهب لـ `/driver-map`
4. اضغط "بدء التتبع"
5. اسمح بالموقع
6. اخرج للمكان المفتوح
7. راقب دقة GPS

---

## ✅ للتحديث السريع

```bash
# 1. Commit changes
git add -A
git commit -m "Update"

# 2. Force push if needed
git push --force-with-lease

# 3. Deploy
flyctl deploy --app abride-app
```

---

الرابط: **https://abride-app.fly.dev**

