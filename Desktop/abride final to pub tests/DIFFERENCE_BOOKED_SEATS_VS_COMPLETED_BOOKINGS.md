# الفرق بين "مقاعد محجوزة" و "حجوزات مكتملة"

## 📊 التعريفات

### 1️⃣ مقاعد محجوزة (Total Booked Seats)
**التعريف:** مجموع عدد المقاعد التي تم حجزها في جميع الحجوزات

**الحساب:**
```sql
SELECT SUM(seats_booked) 
FROM bookings 
WHERE driver_id = 'uuid';
```

**مثال:**
```
حجز 1: 2 مقاعد
حجز 2: 3 مقاعد
حجز 3: 1 مقعد
-------------------
مقاعد محجوزة = 6 مقاعد
```

---

### 2️⃣ حجوزات مكتملة (Completed Bookings)
**التعريف:** عدد الحجوزات التي حالتها `completed`

**الحساب:**
```sql
SELECT COUNT(*) 
FROM bookings 
WHERE driver_id = 'uuid' 
AND status = 'completed';
```

**مثال:**
```
حجز 1: completed ✅
حجز 2: completed ✅
حجز 3: pending ⏳
-------------------
حجوزات مكتملة = 2 حجز
```

---

## 🔍 الفرق الرئيسي

| المقارنة | مقاعد محجوزة | حجوزات مكتملة |
|----------|---------------|----------------|
| **ماذا يُعد؟** | المقاعد | الحجوزات |
| **الوحدة** | مقعد | حجز |
| **الحساب** | `SUM(seats_booked)` | `COUNT(*)` |
| **الحالة** | جميع الحالات* | `completed` فقط |
| **SQL** | `SUM()` | `COUNT()` |

*حسب الكود الحالي

---

## 💡 أمثلة توضيحية

### مثال 1: راكب واحد يحجز عدة مقاعد

```
السيناريو:
- راكب واحد يحجز 4 مقاعد لعائلته
- الحجز مكتمل

النتيجة:
- مقاعد محجوزة: 4 مقاعد
- حجوزات مكتملة: 1 حجز
```

### مثال 2: عدة ركاب في نفس الرحلة

```
السيناريو:
- راكب 1: حجز 2 مقاعد (completed)
- راكب 2: حجز 1 مقعد (completed)
- راكب 3: حجز 3 مقاعد (pending)

النتيجة:
- مقاعد محجوزة: 6 مقاعد (2+1+3)
- حجوزات مكتملة: 2 حجز (راكب 1 + راكب 2 فقط)
```

### مثال 3: رحلة كاملة

```
السيناريو:
رحلة من غرداية إلى الجزائر (4 مقاعد)
- راكب 1: حجز 2 مقاعد (completed) ✅
- راكب 2: حجز 1 مقعد (completed) ✅
- راكب 3: حجز 1 مقعد (completed) ✅

النتيجة:
- مقاعد محجوزة: 4 مقاعد (جميع المقاعد)
- حجوزات مكتملة: 3 حجوزات (3 ركاب)
```

---

## 🎯 في الكود الحالي

### في `Profile.tsx`:

```typescript
// مقاعد محجوزة (جميع الحجوزات)
totalBookedSeats = bookingsData?.reduce((total, booking) => {
  return total + (booking.seatsBooked || 0);
}, 0) || 0;

// حجوزات مكتملة (completed فقط)
completedBookingsCount = bookingsData?.filter(
  (b: any) => b.status === 'completed'
).length || 0;
```

---

## 📊 جدول البيانات

### جدول `bookings`:

| id | passenger | seats_booked | status    |
|----|-----------|--------------|-----------|
| 1  | أحمد      | 2            | completed |
| 2  | فاطمة     | 3            | completed |
| 3  | محمد      | 1            | confirmed |
| 4  | سارة      | 2            | pending   |
| 5  | علي       | 1            | cancelled |

### الحسابات:

```sql
-- مقاعد محجوزة (جميع الحالات)
SELECT SUM(seats_booked) FROM bookings;
-- النتيجة: 9 مقاعد (2+3+1+2+1)

-- مقاعد محجوزة (completed فقط)
SELECT SUM(seats_booked) FROM bookings WHERE status = 'completed';
-- النتيجة: 5 مقاعد (2+3)

-- حجوزات مكتملة
SELECT COUNT(*) FROM bookings WHERE status = 'completed';
-- النتيجة: 2 حجوزات (أحمد + فاطمة)
```

---

## 🤔 أيهما أكثر فائدة؟

### للسائق:

| الإحصائية | الفائدة |
|-----------|---------|
| **مقاعد محجوزة** | كم مقعد تم بيعه (الإيرادات) |
| **حجوزات مكتملة** | كم راكب خدمته (عدد العملاء) |

### أمثلة عملية:

```
سيناريو A:
- 1 حجز × 4 مقاعد = 4 مقاعد محجوزة
- 1 راكب (عائلة كبيرة)
- إيرادات: 4 × سعر المقعد

سيناريو B:
- 4 حجوزات × 1 مقعد = 4 مقاعد محجوزة
- 4 ركاب (أفراد)
- إيرادات: 4 × سعر المقعد

النتيجة:
- نفس عدد المقاعد المحجوزة (4)
- نفس الإيرادات
- لكن عدد الحجوزات مختلف!
```

---

## 📈 في الواجهة

### كيف تظهر:

```
┌─────────────────────────────┐
│  إحصائيات السائق           │
├─────────────────────────────┤
│  مقاعد محجوزة      158     │  ← SUM(seats_booked)
│  حجوزات مكتملة      12     │  ← COUNT(*) WHERE completed
│  إجمالي الرحلات      5     │  ← COUNT(trips)
└─────────────────────────────┘
```

---

## 🔄 العلاقة بينهما

### مثال واقعي:

```
لديك 5 رحلات:
├─ رحلة 1: 3 حجوزات (5 مقاعد) - completed
├─ رحلة 2: 2 حجوزات (4 مقاعد) - completed
├─ رحلة 3: 4 حجوزات (7 مقاعد) - scheduled
├─ رحلة 4: 1 حجز (3 مقاعد) - scheduled
└─ رحلة 5: 2 حجوزات (5 مقاعد) - cancelled

النتائج:
- إجمالي المقاعد المحجوزة: 24 مقعد
- إجمالي الحجوزات المكتملة: 5 حجوزات (رحلة 1 + 2)
- إجمالي الرحلات: 5 رحلات
```

---

## ✅ الخلاصة

### مقاعد محجوزة:
- 📍 **ماذا:** مجموع المقاعد
- 🔢 **كيف:** `SUM(seats_booked)`
- 💰 **لماذا:** لحساب الإيرادات
- 📊 **مثال:** 158 مقعد

### حجوزات مكتملة:
- 📍 **ماذا:** عدد الحجوزات
- 🔢 **كيف:** `COUNT(*) WHERE completed`
- 👥 **لماذا:** لحساب عدد العملاء
- 📊 **مثال:** 12 حجز

---

## 🎯 متى تستخدم أيهما؟

### استخدم "مقاعد محجوزة" لـ:
- ✅ حساب الإيرادات
- ✅ معرفة الإشغال
- ✅ إحصائيات المبيعات

### استخدم "حجوزات مكتملة" لـ:
- ✅ عدد العملاء
- ✅ عدد المعاملات
- ✅ إحصائيات الخدمة

---

## 📝 في SQL

```sql
-- مقاعد محجوزة
SELECT 
    driver_id,
    SUM(seats_booked) AS total_seats,
    'مقاعد محجوزة' AS metric
FROM bookings
GROUP BY driver_id;

-- حجوزات مكتملة
SELECT 
    driver_id,
    COUNT(*) AS completed_count,
    'حجوزات مكتملة' AS metric
FROM bookings
WHERE status = 'completed'
GROUP BY driver_id;
```

---

**باختصار:**
- **مقاعد محجوزة** = كم مقعد؟ (Quantity)
- **حجوزات مكتملة** = كم حجز؟ (Count)

مثل الفرق بين:
- "بعت 10 كيلو تفاح" (مقاعد)
- "خدمت 3 عملاء" (حجوزات)

