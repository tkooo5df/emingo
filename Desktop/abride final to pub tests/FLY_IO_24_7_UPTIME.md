# تفعيل التشغيل المستمر 24/7 على Fly.io

## ✅ التعديلات المطبقة

### 1. إعدادات fly.toml
تم تعديل ملف `fly.toml` لضمان عمل التطبيق 24/7:

```toml
[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false    # ✅ تم تعطيل الإيقاف التلقائي
  auto_start_machines = true
  min_machines_running = 1       # ✅ على الأقل آلة واحدة تعمل دائماً
  processes = ["app"]
```

### 2. التغييرات الأساسية
- **auto_stop_machines**: تم تغييره من `true` إلى `false`
  - هذا يمنع التطبيق من التوقف عند عدم النشاط
- **min_machines_running**: تم تغييره من `0` إلى `1`
  - هذا يضمن وجود آلة واحدة على الأقل تعمل في جميع الأوقات

## 📊 الحالة الحالية

### معلومات التطبيق
- **اسم التطبيق**: abride-app
- **الرابط**: https://abride-app.fly.dev/
- **المنطقة**: cdg (Paris, France)
- **حالة التشغيل**: ✅ مستمر 24/7

### الآلات العاملة
```
Machine 1: 90807244ce34e8
- الحالة: started (✅ تعمل)
- الفحوصات: 1 passing

Machine 2: d8d4242a2d2458
- الحالة: started (✅ تعمل)
- الفحوصات: 1 passing
```

## 💰 ملاحظات التكلفة

⚠️ **مهم جداً**: مع التشغيل 24/7، ستكون هناك تكاليف شهرية مستمرة من Fly.io:

- **الآلات**: $0.0000008/ثانية لكل آلة (حوالي $2.07/شهر لكل آلة)
- **عدد الآلات**: 2 آلات = حوالي $4.14/شهر
- **الذاكرة**: 256MB لكل آلة
- **CPU**: 1 shared CPU لكل آلة

### البدائل لتقليل التكلفة:
1. **تقليل عدد الآلات إلى 1**:
   ```bash
   flyctl machine stop d8d4242a2d2458
   flyctl scale count 1
   ```

2. **استخدام الإيقاف التلقائي في أوقات الذروة المنخفضة**:
   - يمكنك إعادة تفعيل `auto_stop_machines = true` للأوقات التي لا تتوقع فيها حركة مرور

## 🔍 التحقق من الحالة

### التحقق من حالة التطبيق:
```bash
flyctl status
```

### التحقق من السجلات (logs):
```bash
flyctl logs
```

### التحقق من استخدام الموارد:
```bash
flyctl machine list
```

### إعادة تشغيل الآلات يدوياً:
```bash
flyctl machine restart 90807244ce34e8
flyctl machine restart d8d4242a2d2458
```

## 🌐 الوصول إلى التطبيق

التطبيق متاح الآن على:
- **الرابط الرئيسي**: https://abride-app.fly.dev/
- **نوع الاتصال**: HTTPS مفعّل تلقائياً
- **وقت التشغيل**: 24/7 بدون توقف

## ⚙️ الإعدادات المتقدمة

### زيادة الذاكرة (إذا لزم الأمر):
```bash
flyctl scale memory 512
```

### زيادة عدد CPUs:
```bash
flyctl scale vm shared-cpu-2x
```

### إضافة IPv4 مخصص:
```bash
flyctl ips allocate-v4
```

## 🔄 التحديث والنشر

عند إجراء تعديلات جديدة، استخدم:
```bash
git add .
git commit -m "your message"
flyctl deploy
```

التطبيق سيستمر في العمل أثناء النشر بفضل استراتيجية "rolling deployment".

## ✅ التأكد من التشغيل 24/7

يمكنك التأكد من أن التطبيق يعمل بشكل مستمر عبر:
1. زيارة https://abride-app.fly.dev/ في أي وقت
2. التحقق من `flyctl status` - يجب أن تكون الحالة "started"
3. مراقبة التطبيق: https://fly.io/apps/abride-app/monitoring

---

**تم التنفيذ**: 25 أكتوبر 2025  
**الحالة**: ✅ يعمل 24/7

