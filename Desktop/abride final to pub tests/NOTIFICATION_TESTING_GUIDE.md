# دليل اختبار نظام الإشعارات 🔔

## المشاكل التي تم إصلاحها

### ❌ المشاكل السابقة:
1. عدم تحميل الإشعارات تلقائياً عند تسجيل الدخول
2. استخدام `is_read` بدلاً من `isRead` (مشكلة في التسمية)
3. عدم وجود `useEffect` لتحميل الإشعارات
4. عدم تحديث الإشعارات بشكل دوري

### ✅ الإصلاحات:
1. ✅ إضافة `useEffect` لتحميل الإشعارات تلقائياً عند تسجيل الدخول
2. ✅ تصحيح `is_read` إلى `isRead`
3. ✅ إضافة تحديث تلقائي كل 30 ثانية
4. ✅ إضافة console.log لتتبع المشاكل
5. ✅ إضافة إغلاق تلقائي للقائمة المنسدلة عند النقر

## كيفية اختبار الإشعارات محلياً

### الخطوة 1: تشغيل التطبيق محلياً
```bash
npm run dev
```

### الخطوة 2: تسجيل الدخول
1. افتح المتصفح على `http://localhost:5173`
2. سجل الدخول بحساب تجريبي

### الخطوة 3: فتح صفحة الاختبار
1. افتح ملف `test-notifications-local.html` في المتصفح
2. تأكد من تسجيل الدخول في التطبيق الرئيسي أولاً
3. ستظهر معلومات المستخدم الحالي

### الخطوة 4: إنشاء إشعارات تجريبية
اضغط على الأزرار المختلفة لإنشاء إشعارات:
- 📋 إشعارات الحجز (حجز جديد، تأكيد، إلغاء)
- 🚗 إشعارات الرحلات (رحلة جديدة، تحديث)
- 💰 إشعارات الدفع

### الخطوة 5: التحقق من الإشعارات
1. ارجع إلى التطبيق الرئيسي `http://localhost:5173`
2. انظر إلى أيقونة الجرس 🔔 في الـ Header
3. يجب أن ترى عدد الإشعارات غير المقروءة
4. اضغط على الأيقونة لعرض الإشعارات

## التحقق من Console

افتح Developer Tools (F12) وانظر إلى Console:

```
✅ User authenticated, loading notifications...
✅ Loading notifications for user: <user-id>
✅ Loaded notifications: [...]
```

## الدوال المتاحة في نظام الإشعارات

### إنشاء الإشعارات:
```javascript
// إشعار حجز جديد
NotificationService.notifyBookingCreated({
  bookingId: '123',
  passengerId: 'user-id',
  driverId: 'driver-id',
  tripId: 'trip-id',
  pickupLocation: 'غرداية',
  destinationLocation: 'الجزائر',
  seatsBooked: 3,
  totalAmount: 600,
  paymentMethod: 'cod'
});

// إشعار تأكيد حجز
NotificationService.notifyBookingConfirmed('booking-id', 'driver-id');

// إشعار إلغاء حجز
NotificationService.notifyBookingCancelled('booking-id', 'user-id', 'سبب الإلغاء');

// إشعار استلام دفع
NotificationService.notifyPaymentReceived({
  bookingId: 'booking-id',
  amount: 600,
  paymentMethod: 'نقداً',
  payerId: 'passenger-id',
  recipientId: 'driver-id'
});
```

### إدارة الإشعارات:
```javascript
// الحصول على إشعارات المستخدم
const notifications = await NotificationService.getUserNotifications(userId);

// تحديد إشعار كمقروء
await NotificationService.markAsRead(notificationId);

// حذف إشعار
await NotificationService.deleteNotification(notificationId);

// الحصول على عدد الإشعارات غير المقروءة
const count = await NotificationService.getUnreadNotificationsCount(userId);
```

## نظام الأولويات

- 🔴 **CRITICAL** - طوارئ (تظهر حتى في وضع عدم الإزعاج)
- 🟠 **URGENT** - عاجل (تحتاج إجراء فوري)
- 🟡 **HIGH** - مهم (حساس للوقت)
- 🟢 **MEDIUM** - عادي (مهم لكن ليس عاجل)
- ⚪ **LOW** - معلومات (تحديثات عامة)

## التصنيفات

- 📋 **BOOKING** - إشعارات الحجوزات
- 🚗 **TRIP** - إشعارات الرحلات
- 💰 **PAYMENT** - إشعارات الدفع
- 👤 **USER** - إشعارات المستخدمين
- ⚙️ **SYSTEM** - إشعارات النظام
- 🛡️ **SAFETY** - إشعارات الأمان

## سيناريوهات الاختبار

### سيناريو 1: حجز جديد (من منظور السائق)
1. افتح صفحة الاختبار
2. اضغط "إنشاء إشعار حجز جديد"
3. ارجع للتطبيق
4. يجب أن ترى: 🚗 حجز جديد

### سيناريو 2: تأكيد حجز (من منظور الراكب)
1. اضغط "إنشاء إشعار تأكيد حجز"
2. يجب أن ترى: ✅ تم تأكيد الحجز

### سيناريو 3: إشعارات متعددة
1. أنشئ عدة إشعارات
2. يجب أن يظهر العدد الصحيح على الأيقونة
3. الإشعارات غير المقروءة تظهر بلون مختلف

## استكشاف الأخطاء

### لا تظهر الإشعارات؟
1. تأكد من تسجيل الدخول
2. افتح Console وابحث عن أخطاء
3. تأكد من وجود `demo_user` في localStorage
4. تحقق من `demo_database` في localStorage

### الإشعارات لا تتحدث؟
1. احذف الإشعارات القديمة: اضغط "مسح جميع الإشعارات"
2. أنشئ إشعارات جديدة
3. انتظر 30 ثانية (التحديث التلقائي)
4. أو حدّث الصفحة

### عدد الإشعارات غير صحيح؟
```javascript
// في Console:
localStorage.getItem('demo_database')
// تحقق من محتوى notifications
```

## الملفات المعدلة

- ✅ `src/components/layout/Header.tsx` - إصلاح تحميل الإشعارات
- ✅ `test-notifications-local.html` - أداة اختبار
- ✅ `NOTIFICATION_TESTING_GUIDE.md` - هذا الدليل

## النتيجة المتوقعة

عند عمل النظام بشكل صحيح:
- ✅ الإشعارات تظهر تلقائياً عند تسجيل الدخول
- ✅ العدد الصحيح يظهر على أيقونة الجرس
- ✅ النقر على إشعار يحدده كمقروء
- ✅ التحديث التلقائي كل 30 ثانية
- ✅ console.log يظهر معلومات مفيدة

## ملاحظات مهمة

⚠️ **لا ترفع التحديثات إلى Fly.io حتى تتأكد من عمل النظام محلياً!**

🔧 **التطوير محلي فقط** - اختبر جميع السيناريوهات أولاً

📝 **استخدم console.log** - لمتابعة سير العمل

---

**آخر تحديث:** 27 أكتوبر 2025  
**الحالة:** جاهز للاختبار المحلي ✅


