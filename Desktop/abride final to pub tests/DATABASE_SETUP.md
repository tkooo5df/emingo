# إعداد قاعدة بيانات Supabase لنظام DZ Taxi

يعتمد التطبيق الآن اعتمادًا كاملًا على Supabase لتخزين المستخدمين، الرحلات، الحجوزات، والإشعارات. إذا تم إنشاء مشروع Supabase جديد دون تشغيل جميع الهجرات السابقة فستظهر أخطاء أثناء التسجيل أو عند إرسال الإشعارات. اتبع هذه الخطوات لإعادة تهيئة قاعدة البيانات بشكل صحيح.

---

## 📦 المتطلبات المسبقة

1. **حساب Supabase نشط** مع مشروع جاهز.
2. **Supabase CLI** مثبت محليًا (اختياري لكنه مفضل):
   ```bash
   npm install -g supabase
   ```
3. الوصول إلى مجلد المشروع الذي يحتوي على ملفات `supabase/migrations`.

---

## 🚀 تهيئة المخطط عبر Supabase CLI

1. **تسجيل الدخول**
   ```bash
   supabase login
   ```
2. **ربط CLI بالمشروع**
   ```bash
   supabase link --project-ref YOUR_PROJECT_REF
   ```
   > استبدل `YOUR_PROJECT_REF` بالمعرف الخاص بمشروعك في Supabase.
3. **تشغيل جميع الهجرات**
   ```bash
   supabase db push
   ```
   يقوم هذا الأمر بتطبيق كامل الملفات داخل `supabase/migrations`، بما في ذلك سكربت الإعداد الكامل `20260206000000_supabase_full_reset.sql`.
4. **إعادة تشغيل التطبيق** حتى يقرأ المخطط الجديد.

---

## 🖥️ تشغيل السكربت يدويًا عبر SQL Editor

إذا لم تستطع استخدام CLI، انسخ محتوى الملف التالي بالكامل وشغّله داخل SQL Editor في لوحة Supabase:
```
supabase/migrations/20260206000000_supabase_full_reset.sql
```
يشمل هذا الملف كل مما يلي:
* إنشاء جدول `profiles` مع أعمدة الاسم، اللغة، صورة الحساب، ودعم أدوار `passenger`, `driver`, `admin`, `developer`.
* تفعيل المحفزات التي تنشئ ملف المستخدم تلقائيًا (`handle_new_user`) وتحدّث حقول `updated_at`.
* إنشاء جدول `notifications` بجميع الحقول المستخدمة في التطبيق، مع سياسات RLS تسمح للمستخدم بإدارة إشعاراته وللمسؤولين/المطورين بمراجعتها.
* تجهيز جداول `vehicles`, `trips`, `bookings`, `system_settings`, و`admin_logs` مع كل الفهارس الضرورية.
* إدراج إعدادات افتراضية للنظام حتى تعمل لوحة التحكم مباشرة بعد الإعداد.

---

## 🔍 التحقق بعد التنفيذ

نفّذ الاستعلامات التالية داخل SQL Editor للتأكد من نجاح الإعداد:
```sql
select count(*) from profiles;
select count(*) from notifications;
select count(*) from system_settings;
```
إذا عادت الاستعلامات السابقة بأرقام دون أخطاء، فالمخطط جاهز. بعدها:
1. أعد تشغيل تطبيق الويب أو الواجهة الخلفية.
2. جرّب التسجيل بحساب جديد للتأكد من اختفاء خطأ Supabase 500.
3. تحقق من لوحة التحكم للتأكد من ظهور الاسم الحقيقي والإشعارات.

---

## 🆘 ماذا أفعل إذا ظهرت أخطاء جديدة؟

1. تأكد من أنك نفذت السكربت بالكامل (لا تقطع التنفيذ في منتصفه).
2. راجع سجلات Supabase من تبويب **Logs** لأي رسالة خطأ إضافية.
3. إذا استمر الخطأ، انسخ نص الرسالة الحرفي وشاركني إياه لنساعدك في تشخيص المشكلة.

باتباع الخطوات أعلاه يصبح مشروع DZ Taxi متكاملاً مع Supabase دون الحاجة إلى قاعدة بيانات محلية.
