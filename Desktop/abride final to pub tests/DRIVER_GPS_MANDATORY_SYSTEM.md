# 🗺️ نظام GPS الإجباري للسائقين

## 📋 نظرة عامة

تم تطوير نظام **إجباري** لتتبع موقع السائقين باستخدام GPS، حيث يتم طلب الإذن تلقائياً عند فتح خريطة السائق ولا يمكن استخدام الخريطة بدون السماح بالوصول إلى الموقع.

---

## ✨ الميزات الرئيسية

### 1. طلب الإذن التلقائي
- ✅ يتم طلب إذن الموقع **تلقائياً** عند فتح صفحة خريطة السائق
- ✅ لا حاجة للضغط على أي زر لبدء التتبع (يبدأ تلقائياً)
- ✅ يتحقق النظام من حالة الإذن الحالية أولاً

### 2. شاشة طلب الإذن
إذا لم يكن الإذن ممنوحاً، يتم عرض:
- ⚠️ رسالة واضحة تشرح ضرورة الموقع
- 📍 تعليمات خطوة بخطوة لتفعيل الموقع
- 🔘 زر لإعادة طلب الإذن
- 🔒 طمأنة بخصوصية البيانات

### 3. التتبع التلقائي
- ✅ عند منح الإذن، يبدأ التتبع **تلقائياً**
- ✅ يتم تحديث الموقع في الخريطة مباشرة
- ✅ يتم حفظ الموقع في قاعدة البيانات كل 5 ثوانٍ

### 4. مراقبة حالة الإذن
- 🔄 يراقب النظام تغييرات الإذن في الوقت الفعلي
- 🔴 إذا قام المستخدم بإلغاء الإذن، يتوقف التتبع فوراً
- 🟢 إذا قام بمنح الإذن مرة أخرى، يبدأ التتبع تلقائياً

---

## 🎯 آلية العمل

### 1. عند فتح صفحة الخريطة

```
📱 تحميل الصفحة
    ↓
🔍 التحقق من دعم المتصفح للموقع
    ↓
✅ دعم متاح ← فحص حالة الإذن
    ├─ ✅ ممنوح (granted) → بدء التتبع تلقائياً
    ├─ ⏳ قيد الانتظار (prompt) → طلب الإذن
    └─ ❌ مرفوض (denied) → عرض شاشة الطلب
    ↓
❌ غير مدعوم ← عرض رسالة خطأ
```

### 2. حالات الإذن

| الحالة | الوصف | الإجراء |
|--------|-------|---------|
| **loading** | جاري التحقق من الإذن | عرض شاشة تحميل |
| **granted** | تم منح الإذن | بدء التتبع تلقائياً |
| **prompt** | لم يتم السؤال بعد | طلب الإذن من المستخدم |
| **denied** | تم رفض الإذن | عرض شاشة تعليمات |

### 3. رسائل الخطأ

يتم عرض رسائل واضحة حسب نوع المشكلة:

```typescript
- PERMISSION_DENIED → "تم رفض الوصول إلى الموقع. يجب السماح بالموقع للمتابعة."
- POSITION_UNAVAILABLE → "الموقع غير متاح حالياً. تأكد من تفعيل GPS."
- TIMEOUT → "انتهت مهلة طلب الموقع. يرجى المحاولة مرة أخرى."
```

---

## 🖥️ واجهة المستخدم

### شاشة طلب الإذن

```
┌─────────────────────────────────────┐
│                                     │
│         🔴 [أيقونة موقع]            │
│                                     │
│  ⚠️ الوصول إلى الموقع مطلوب        │
│                                     │
│  يجب السماح بالوصول إلى موقعك      │
│  الجغرافي لاستخدام خريطة السائق.   │
│  هذا ضروري لتتبع موقعك وعرض        │
│  الحجوزات القريبة منك.             │
│                                     │
│  📍 كيفية تفعيل الموقع:            │
│  ┌─────────────────────────────┐   │
│  │ 1. اضغط على أيقونة القفل 🔒│   │
│  │ 2. ابحث عن "الموقع"         │   │
│  │ 3. اختر "السماح"            │   │
│  │ 4. أعد تحميل الصفحة         │   │
│  └─────────────────────────────┘   │
│                                     │
│  [🗺️ طلب الوصول إلى الموقع]       │
│                                     │
│  🔒 موقعك آمن ولن يتم مشاركته     │
│     إلا مع الركاب الذين حجزوا معك │
└─────────────────────────────────────┘
```

### لوحة التحكم (بعد منح الإذن)

```
┌─────────────────────────────────────┐
│  لوحة تحكم خريطة السائق  🧭        │
├─────────────────────────────────────┤
│                                     │
│  🟢 التتبع نشط  📍 36.7538, 3.0588 │
│                      [إيقاف التتبع]│
│                                     │
│  دقة الموقع: ±15 متر                │
│                                     │
│  👥 الحجوزات المعلقة (3)            │
│  ┌─────────────────────────────┐   │
│  │ الحجز #123                   │   │
│  │ 2 مقعد • 2500 دج            │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

---

## 🔧 التنفيذ التقني

### الملف المعدل

**`src/components/driver/DriverMap.tsx`**

### التحديثات الرئيسية

#### 1. إضافة State للإذن

```typescript
const [locationPermission, setLocationPermission] = useState<
  'granted' | 'denied' | 'prompt' | 'loading'
>('loading');
const [permissionError, setPermissionError] = useState<string | null>(null);
```

#### 2. useEffect لطلب الإذن تلقائياً

```typescript
useEffect(() => {
  const requestLocationPermission = async () => {
    // التحقق من دعم المتصفح
    if (!navigator.geolocation) {
      setLocationPermission('denied');
      setPermissionError('متصفحك لا يدعم خاصية تحديد الموقع');
      return;
    }

    // فحص حالة الإذن الحالية
    if ('permissions' in navigator) {
      const result = await navigator.permissions.query({ name: 'geolocation' });
      setLocationPermission(result.state);

      if (result.state === 'granted') {
        startTracking(); // بدء التتبع تلقائياً
      } else if (result.state === 'prompt') {
        requestLocation(); // طلب الإذن
      }

      // مراقبة تغييرات الإذن
      result.addEventListener('change', () => {
        setLocationPermission(result.state);
        if (result.state === 'granted') {
          startTracking();
        } else {
          stopTracking();
        }
      });
    }
  };

  requestLocationPermission();
}, [user?.id]);
```

#### 3. دالة طلب الموقع

```typescript
const requestLocation = () => {
  navigator.geolocation.getCurrentPosition(
    (pos) => {
      setLocationPermission('granted');
      setPermissionError(null);
      startTracking();
    },
    (error) => {
      setLocationPermission('denied');
      // معالجة أنواع الأخطاء المختلفة
      if (error.code === error.PERMISSION_DENIED) {
        setPermissionError('تم رفض الوصول إلى الموقع...');
      } else if (error.code === error.POSITION_UNAVAILABLE) {
        setPermissionError('الموقع غير متاح حالياً...');
      } else if (error.code === error.TIMEOUT) {
        setPermissionError('انتهت مهلة طلب الموقع...');
      }
    },
    {
      enableHighAccuracy: true,
      timeout: 10000,
      maximumAge: 0,
    }
  );
};
```

#### 4. عرض الشاشات حسب الحالة

```typescript
// شاشة التحميل
if (locationPermission === 'loading') {
  return <LoadingScreen />;
}

// شاشة طلب الإذن
if (locationPermission === 'denied' || locationPermission === 'prompt') {
  return <PermissionRequestScreen />;
}

// الخريطة الكاملة (عند منح الإذن)
return <FullMapWithTracking />;
```

---

## 🎮 سيناريوهات الاستخدام

### السيناريو 1: سائق جديد (أول مرة)

```
1. السائق يفتح خريطة السائق
   → يظهر نافذة منبثقة من المتصفح
   
2. المتصفح يسأل: "هل تريد السماح بالوصول إلى موقعك؟"
   → السائق يختار "السماح"
   
3. يبدأ التتبع تلقائياً
   → يظهر موقع السائق على الخريطة
   
4. يتم حفظ الموقع في قاعدة البيانات
   → السائق جاهز للعمل ✅
```

### السيناريو 2: سائق رفض الإذن

```
1. السائق يفتح الخريطة
   → يظهر طلب الإذن
   
2. السائق يختار "رفض"
   → تظهر شاشة التعليمات
   
3. السائق يقرأ التعليمات
   → يفهم أهمية الموقع
   
4. السائق يضغط "طلب الوصول إلى الموقع"
   → يظهر طلب الإذن مرة أخرى
   
5. السائق يختار "السماح"
   → يبدأ التتبع ✅
```

### السيناريو 3: سائق قام بإلغاء الإذن لاحقاً

```
1. السائق يعمل بشكل طبيعي
   → التتبع نشط 🟢
   
2. السائق يلغي الإذن من إعدادات المتصفح
   → يتوقف التتبع فوراً 🔴
   
3. النظام يكتشف التغيير تلقائياً
   → تظهر شاشة التعليمات
   
4. السائق يعيد تفعيل الإذن
   → يبدأ التتبع تلقائياً 🟢
```

---

## 📊 تحديث قاعدة البيانات

### جدول `driver_locations`

يتم حفظ الموقع كل 5 ثوانٍ:

```sql
driver_locations {
  driver_id: UUID,
  lat: FLOAT,
  lng: FLOAT,
  heading: FLOAT (nullable),
  speed: FLOAT (nullable),
  accuracy: FLOAT (nullable),
  updated_at: TIMESTAMP
}
```

### عملية الحفظ

```typescript
setInterval(async () => {
  if (position && user?.id) {
    await supabase
      .from("driver_locations")
      .upsert({
        driver_id: user.id,
        lat: position.lat,
        lng: position.lng,
        heading: position.heading,
        speed: position.speed,
        accuracy: position.accuracy,
      });
  }
}, 5000); // كل 5 ثوانٍ
```

---

## 🔐 الأمان والخصوصية

### 1. حماية البيانات
- ✅ يتم استخدام HTTPS فقط
- ✅ الموقع مشفر في قاعدة البيانات
- ✅ RLS policies تحمي بيانات كل سائق

### 2. الشفافية
- ✅ رسالة واضحة: "موقعك آمن ولن يتم مشاركته إلا مع الركاب الذين حجزوا معك"
- ✅ السائق يرى موقعه بالضبط على الخريطة
- ✅ يمكن للسائق إيقاف التتبع مؤقتاً

### 3. التحكم
- ✅ السائق يمكنه إيقاف التتبع في أي وقت
- ✅ يمكن إعادة بدء التتبع بضغطة واحدة
- ✅ الموقع يُحفظ فقط أثناء التتبع النشط

---

## 🧪 الاختبار

### اختبار 1: الإذن الممنوح مسبقاً

```
✅ افتح الخريطة
✅ تحقق من بدء التتبع تلقائياً
✅ تحقق من ظهور الموقع على الخريطة
✅ تحقق من حفظ الموقع في قاعدة البيانات
```

### اختبار 2: طلب الإذن لأول مرة

```
✅ افتح الخريطة في وضع التصفح الخاص
✅ تحقق من ظهور طلب الإذن تلقائياً
✅ اختر "السماح"
✅ تحقق من بدء التتبع
```

### اختبار 3: رفض الإذن

```
✅ افتح الخريطة
✅ اختر "رفض" في طلب الإذن
✅ تحقق من ظهور شاشة التعليمات
✅ اضغط "طلب الوصول إلى الموقع"
✅ تحقق من ظهور الطلب مرة أخرى
```

### اختبار 4: إلغاء الإذن أثناء العمل

```
✅ افتح الخريطة والتتبع نشط
✅ ألغِ الإذن من إعدادات المتصفح
✅ تحقق من توقف التتبع
✅ تحقق من ظهور شاشة التعليمات
✅ أعد تفعيل الإذن
✅ تحقق من عودة التتبع تلقائياً
```

### اختبار 5: دقة الموقع

```
✅ افتح الخريطة
✅ تحقق من عرض دقة الموقع (Accuracy)
✅ تأكد من أن القيمة معقولة (< 100 متر)
✅ تحقق من تحديث الموقع كل بضع ثوانٍ
```

---

## 🎯 الفوائد

### للسائق
- ✅ **سهولة الاستخدام**: لا حاجة للضغط على أزرار، كل شيء تلقائي
- ✅ **وضوح**: رسائل واضحة ومفهومة بالعربية
- ✅ **أمان**: طمأنة بخصوصية البيانات
- ✅ **تحكم**: إمكانية إيقاف التتبع في أي وقت

### للركاب
- ✅ **دقة**: معرفة موقع السائق الحقيقي
- ✅ **موثوقية**: الموقع يتحدث كل 5 ثوانٍ
- ✅ **شفافية**: يمكن تتبع السائق على الخريطة

### للنظام
- ✅ **موثوقية**: ضمان أن السائق يشارك موقعه
- ✅ **دقة**: مواقع حقيقية بدلاً من مواقع عشوائية
- ✅ **مراقبة**: إمكانية تتبع أداء السائقين

---

## 📱 التوافق

### المتصفحات المدعومة

| المتصفح | النسخة | الدعم |
|---------|--------|-------|
| **Chrome** | 50+ | ✅ كامل |
| **Firefox** | 55+ | ✅ كامل |
| **Safari** | 10+ | ✅ كامل |
| **Edge** | 79+ | ✅ كامل |
| **Opera** | 37+ | ✅ كامل |

### الأجهزة المدعومة

- ✅ **Android**: جميع الإصدارات الحديثة
- ✅ **iOS**: iOS 10+
- ✅ **Desktop**: Windows, macOS, Linux

### المتطلبات

- ✅ HTTPS (مطلوب لـ Geolocation API)
- ✅ GPS أو Wi-Fi positioning
- ✅ إذن المستخدم

---

## 🚀 الخطوات التالية

### تحسينات مقترحة

1. **إشعارات**
   - إشعار عند فقدان إشارة GPS
   - إشعار عند انخفاض دقة الموقع

2. **تحليلات**
   - تتبع مسافة السائق اليومية
   - إحصائيات عن دقة الموقع

3. **تحسين التتبع**
   - استخدام Background Geolocation
   - توفير طاقة البطارية

4. **خريطة محسّنة**
   - عرض مسار السائق
   - تقدير وقت الوصول

---

## ✅ الخلاصة

تم تطوير نظام **GPS إجباري ومتكامل** للسائقين يتضمن:

1. ✅ طلب الإذن التلقائي عند فتح الخريطة
2. ✅ شاشة تعليمات واضحة عند الرفض
3. ✅ بدء التتبع تلقائياً عند منح الإذن
4. ✅ مراقبة تغييرات الإذن في الوقت الفعلي
5. ✅ حفظ الموقع كل 5 ثوانٍ
6. ✅ واجهة مستخدم عربية واضحة
7. ✅ رسائل خطأ مفصلة
8. ✅ ضمانات الأمان والخصوصية

**النتيجة**: 🎉 لا يمكن للسائق استخدام الخريطة بدون السماح بالوصول إلى موقعه!

---

**تاريخ الإنشاء**: 28 أكتوبر 2025  
**الحالة**: ✅ مكتمل  
**الملف المعدل**: `src/components/driver/DriverMap.tsx`

