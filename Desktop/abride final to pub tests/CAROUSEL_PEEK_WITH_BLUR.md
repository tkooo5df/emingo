# ✅ إضافة Peek View مع Blur للكاروسيل

## 🎯 الميزة الجديدة

الآن الكاروسيل يعرض:
- 📍 **البطاقة الرئيسية** في المنتصف (كاملة)
- 👀 **10% من البطاقات الجانبية** (peek)
- 🌫️ **Blur أبيض** على الجوانب للتركيز

---

## 🎨 التصميم

### قبل ❌:
```
[بطاقة 1] [بطاقة 2] [بطاقة 3]
← كل البطاقات متساوية، لا تركيز
```

### بعد ✅:
```
🌫️ [10%] [البطاقة الرئيسية 100%] [10%] 🌫️
      ↑              ↑              ↑
   جانبية       التركيز هنا      جانبية
```

---

## 💻 ما تم إضافته؟

### 1. **Blur Overlays**
```tsx
{/* Blur Overlay اليسار */}
<div className="absolute left-0 top-0 bottom-0 w-32 
  bg-gradient-to-r from-background/80 via-background/40 to-transparent 
  backdrop-blur-sm z-10 pointer-events-none hidden md:block" 
/>

{/* Blur Overlay اليمين */}
<div className="absolute right-0 top-0 bottom-0 w-32 
  bg-gradient-to-l from-background/80 via-background/40 to-transparent 
  backdrop-blur-sm z-10 pointer-events-none hidden md:block" 
/>
```

**المميزات:**
- `backdrop-blur-sm` - تأثير blur خفيف
- `from-background/80` - يبدأ كثيف 80%
- `via-background/40` - يصبح خفيف 40%
- `to-transparent` - ينتهي شفاف
- `pointer-events-none` - لا يمنع النقر
- `hidden md:block` - فقط على Desktop

### 2. **Center Alignment**
```tsx
opts={{
  align: "center",  // ← من "start" إلى "center"
  loop: true,
  dragFree: false,
  direction: "rtl",
}}
```

**الفائدة:**
- البطاقة الرئيسية في **المنتصف**
- البطاقات الجانبية تظهر جزئياً
- تجربة مستخدم أفضل

### 3. **Peek Sizing**
```tsx
basis-[85%] md:basis-[60%] lg:basis-[45%]
```

**على الموبايل:**
- `85%` عرض البطاقة
- `15%` يظهر من البطاقة التالية (7.5% على كل جانب)

**على Tablet:**
- `60%` عرض البطاقة
- `40%` يظهر من البطاقات الجانبية (20% على كل جانب)

**على Desktop:**
- `45%` عرض البطاقة
- `55%` يظهر من البطاقات الجانبية (~27% على كل جانب)

---

## 🎯 تجربة المستخدم

### على Desktop 🖥️:

```
    🌫️        🎯 التركيز        🌫️
    ↓             ↓             ↓
┌─────┐   ┌────────────┐   ┌─────┐
│ 10% │   │  البطاقة   │   │ 10% │
│blur │   │  الرئيسية  │   │blur │
└─────┘   └────────────┘   └─────┘
```

### على Mobile 📱:

```
        ┌──────────────┐
  ┌─┐   │   البطاقة    │   ┌─┐
  │7│   │   الرئيسية   │   │7│
  │%│   │   (85%)       │   │%│
  └─┘   └──────────────┘   └─┘
  
  ← لا blur على الموبايل
```

---

## 📊 CSS Classes المستخدمة

### Blur Gradient:
```css
bg-gradient-to-r         /* اتجاه من اليسار لليمين */
from-background/80       /* يبدأ بـ 80% opacity */
via-background/40        /* يمر بـ 40% opacity */
to-transparent          /* ينتهي شفاف */
```

### Backdrop Blur:
```css
backdrop-blur-sm        /* blur خفيف للخلفية */
```

### Z-Index:
```css
z-10                    /* فوق الكاروسيل */
```

### Pointer Events:
```css
pointer-events-none     /* لا يمنع النقر على الكاروسيل */
```

### Responsive:
```css
hidden md:block         /* مخفي على موبايل، ظاهر على desktop */
```

---

## 🎨 التأثيرات البصرية

### Gradient Blur:
- **اليسار:** `gradient-to-r` (من اليسار → يمين)
- **اليمين:** `gradient-to-l` (من اليمين → يسار)
- **العرض:** `w-32` (128px)

### التركيز:
- البطاقة الرئيسية **واضحة 100%**
- البطاقات الجانبية **خافتة** مع blur
- انتقال سلس بين الوضوح والـ blur

---

## 🧪 كيف يعمل؟

### 1. عند فتح الصفحة:
```
البطاقة 1 في المنتصف ✅
10% من البطاقة 2 تظهر على الجانب 👀
Blur أبيض على الجوانب 🌫️
```

### 2. عند السحب يميناً:
```
البطاقة 2 تنتقل للمنتصف ←
البطاقة 1 تصبح جانبية مع blur 🌫️
Autoplay يتوقف 🛑
```

### 3. عند Autoplay:
```
كل 3 ثوانٍ:
البطاقة التالية تنتقل للمنتصف تلقائياً ✨
الانتقال سلس وجميل 🎯
```

---

## 🎯 المميزات

### ✅ تركيز واضح
- البطاقة الرئيسية **بارزة**
- البطاقات الجانبية **خافتة**
- تجربة بصرية ممتازة

### ✅ دلالة بصرية
- المستخدم يرى أن هناك **المزيد**
- 10% peek يشجع على السحب
- Blur يعطي إحساس بالعمق

### ✅ تصميم احترافي
- Gradient ناعم
- Blur خفيف
- لا يؤثر على التفاعل

### ✅ Responsive
- يعمل على Desktop ✅
- مخفي على Mobile (لا حاجة له) ✅
- تجربة محسّنة لكل جهاز

---

## 📱 Responsive Behavior

### Mobile (< 768px):
```tsx
hidden md:block  // ← Blur مخفي
basis-[85%]      // ← بطاقة كبيرة
```
- لا blur (شاشة صغيرة)
- البطاقة 85% من العرض
- 7.5% peek على كل جانب

### Tablet (≥ 768px):
```tsx
md:block         // ← Blur يظهر
basis-[60%]      // ← بطاقة متوسطة
```
- Blur على الجوانب
- البطاقة 60% من العرض
- 20% peek على كل جانب

### Desktop (≥ 1024px):
```tsx
md:block         // ← Blur يظهر
basis-[45%]      // ← بطاقة مناسبة
```
- Blur واضح على الجوانب
- البطاقة 45% من العرض
- ~27% peek على كل جانب

---

## 🎨 التخصيص

### تغيير عرض Blur:
```tsx
w-32   // 128px ← الحالي
w-24   // 96px  ← أقل
w-40   // 160px ← أكثر
```

### تغيير شدة Blur:
```tsx
backdrop-blur-sm    // خفيف ← الحالي
backdrop-blur-md    // متوسط
backdrop-blur-lg    // قوي
```

### تغيير Opacity:
```tsx
from-background/80  // 80% ← الحالي
from-background/90  // 90% ← أكثر كثافة
from-background/60  // 60% ← أقل كثافة
```

### تغيير Peek Size:
```tsx
basis-[85%]  // ← موبايل
basis-[70%]  // أكثر peek
basis-[90%]  // أقل peek

basis-[45%]  // ← desktop
basis-[40%]  // أكثر peek
basis-[50%]  // أقل peek
```

---

## 🔧 الكود الكامل

```tsx
<div className="relative">
  {/* Blur Overlay اليسار */}
  <div className="absolute left-0 top-0 bottom-0 w-32 
    bg-gradient-to-r from-background/80 via-background/40 to-transparent 
    backdrop-blur-sm z-10 pointer-events-none hidden md:block" 
  />
  
  {/* Blur Overlay اليمين */}
  <div className="absolute right-0 top-0 bottom-0 w-32 
    bg-gradient-to-l from-background/80 via-background/40 to-transparent 
    backdrop-blur-sm z-10 pointer-events-none hidden md:block" 
  />
  
  <Carousel
    setApi={setApi}
    opts={{
      align: "center",
      loop: true,
      dragFree: false,
      direction: "rtl",
    }}
    className="w-full max-w-7xl mx-auto"
  >
    <CarouselContent className="-ml-4">
      {ksour.map((ksar, index) => (
        <CarouselItem 
          key={index} 
          className="pl-4 basis-[85%] md:basis-[60%] lg:basis-[45%]"
        >
          {/* محتوى البطاقة */}
        </CarouselItem>
      ))}
    </CarouselContent>
  </Carousel>
</div>
```

---

## ✅ الخلاصة

### ما تم إضافته:
- ✅ **Center alignment** - البطاقة الرئيسية في المنتصف
- ✅ **Peek view** - 10% من البطاقات الجانبية تظهر
- ✅ **Blur overlays** - أبيض على الجوانب للتركيز
- ✅ **Responsive sizing** - مناسب لكل جهاز
- ✅ **Gradient blur** - انتقال ناعم وجميل

### النتيجة:
**الكاروسيل الآن أكثر احترافية وجاذبية!** ✨

- 🎯 التركيز على البطاقة الرئيسية
- 👀 دلالة بصرية على المزيد من المحتوى
- 🌫️ تأثير blur جميل ومريح للعين
- 📱 Responsive على جميع الأجهزة

---

**🎉 الميزة جاهزة! جرّبها الآن!** 🚀

```bash
npm run dev
```

**شاهد التأثير الجميل والتركيز على البطاقة الرئيسية!** ✨

